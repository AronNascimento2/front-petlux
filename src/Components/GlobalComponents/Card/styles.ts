import styled from 'styled-components'

export const Container = styled.div`
  width: 300px;
  height: 300px; /* Tamanho inicial (pode ser ajustado) */
  padding: 16px;
  border-radius: 8px;
  box-shadow: rgba(0, 0, 0, 0.35) 0px 5px 15px;
  background-color: #fff;
  margin-bottom: 16px;
  color: black;
`
